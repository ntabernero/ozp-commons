define([
        'backbone'
],

function(Backbone) {

    var WidgetStateModel = Backbone.Model.extend({

        initialize: function() {
            
        }

    });

    return WidgetStateModel;

});
